import { ApplicationBoard } from "@/features/job-applications/components/application-board";

export const metadata = { title: "지원 관리 | JobCodi" };

export default function ApplicationsPage() {
  return <ApplicationBoard />;
}
